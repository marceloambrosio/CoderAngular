import { SizeTituloDirective } from './size-titulo.directive';

describe('SizeTituloDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeTituloDirective();
    expect(directive).toBeTruthy();
  });
});
